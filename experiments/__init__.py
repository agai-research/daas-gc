"""Experiment scripts reproducing Section 7.3 of DaaS-GC.tex.

Kept fully separate from the daasgc/ and baselines/ prototype code: each
script here only loads a dataset instance, calls the four compared
methods, times/records the outcome, and writes JSON results + figures.
"""
